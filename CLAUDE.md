# Claude Code instructions

Read and follow `AGENTS.md` for all repository work. For requests to create or extend an overlay, also use the
project skill at `.claude/skills/create-engram-overlay/SKILL.md`.

`docs/llm-overlay-authoring.md` is the canonical authoring guide. Do not duplicate or override its protocol,
privacy, backend-selection, or verification rules here.
